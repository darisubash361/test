import 'package:flutter/material.dart';

class Guru extends StatelessWidget {
  const Guru({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Pandit is a laad'),
      ),
    );
  }
}
